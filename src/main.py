from textnode import TextNode, TextType
from htmlnode import ParentNode, LeafNode
def main():
    some_node = TextNode("text hier einfügen", "link", "https://www.boot.dev")
    print(some_node)
                         
























main()

